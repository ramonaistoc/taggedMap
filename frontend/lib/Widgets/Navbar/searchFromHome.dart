import 'package:flutter/material.dart';

class SearchFromHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      margin: const EdgeInsets.all(10.0),
      color: Colors.purple,
      width: 48.0,
      height: 48.0,
    );
  }
}
